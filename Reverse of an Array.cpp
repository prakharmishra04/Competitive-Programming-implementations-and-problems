#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    vector<int> arr;
    for(int i=0 ; i<6; i++)
    {
        arr.push_back(i);
    }
    cout<<arr[5];
    return 0;
}